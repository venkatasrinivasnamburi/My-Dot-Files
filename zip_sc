# zip commands to zip

: <<'END'
 commadn to zip . zip -r archive_name.zip folder_to_compress

 commadn to zip but exclude files in mac like “_MACOSX” or “._Filename” and .ds store files, use the “-X” option . zip -r -X archive_name.zip folder_to_compress

 commadn to unzip the archive .unzip source_folder_name.zip

To compress

tar -zcvf archive_name.tar.gz folder_to_compress

To extract

tar -zxvf archive_name.tar.gz

bz2 : better compression than tar and zip

To compress

tar -jcvf archive_name.tar.bz2 folder_to_compress

To extract

tar -jxvf archive_name.tar.bz2
END
